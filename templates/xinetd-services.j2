service {{ item.name }}
{
    disable         = no
    socket_type     = stream
    protocol        = tcp
    wait            = no
    user            = root
    server          = /bin/bash
    server_args     = {{ctfc_service_dir}}/{{ item.name }}/{{ item.script }}
}
