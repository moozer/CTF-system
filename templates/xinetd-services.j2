service {{ item.name }}
{
    disable         = no
    socket_type     = stream
    protocol        = tcp
    wait            = no
    user            = root
    server          = /bin/bash
    server_args     = {{ ansible_env.HOME}}/{{ item.dest_path }}/{{ item.filename }}
}
