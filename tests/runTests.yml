- hosts: gameclient??
  become: no
  user: "{{ ctf_user }}"
  
  vars_files:
  - roles/ctfd-server/defaults/main.yml
  - roles/ctfd-server/defaults/services.yml
    
  tasks:
  - include: ../tasks/tests.yml
